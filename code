#include <bits/stdc++.h>

using namespace std;


int main()
{
    float v, d, t;

    cin >> v >> d;
    
    v = v * 1000 / 60;
    t = d / v;
    
    cout << ceil(t);
    
    //ceil rotunjeste numarul la cel mai mare numar intreg mai mare ca numarul dat
}
